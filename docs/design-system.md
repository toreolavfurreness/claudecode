# Designsystem

> Skjelett. Slett denne filen hvis prosjektet ikke har UI. Ellers fyll ut faktiske farger, typografi og mønstre.

## Fargepalett

Ikke introduser nye farger uten å spørre først.

| Navn | Hex | Bruksområde |
|------|-----|-------------|
| `<navn>` | `<#hex>` | `<f.eks. primærknapper, CTA>` |
| `<navn>` | `<#hex>` | `<f.eks. bakgrunn>` |
| `<navn>` | `<#hex>` | `<f.eks. feilmeldinger>` |

## Typografi

- **`<Hovedfont>`** — overskrifter, labels, knapper
- **`<Brødtekst-font>`** — brødtekst
- Last inn via `<f.eks. Google Fonts / lokal fil>`

## Designprinsipper

- `<f.eks. mørk bakgrunn med fargerike aksenter>`
- `<f.eks. avrundede hjørner: kort = 16px, knapper = 999px (pill)>`
- `<f.eks. modaler maxWidth 480, alignSelf 'center'>`
- `<andre prinsipper som er konsistent gjennom appen>`

## Kategori- eller domenespesifikke fargemapper *(slett hvis ikke relevant)*

`<Hvis prosjektet har domeneknyttede fargevalg — f.eks. status-farger, kategori-farger —
dokumenter dem som en mapping så valgene er reproduserbare.>`

| `<Domene>` | Farge | Hex |
|------------|-------|-----|
| `<verdi>` | `<navn>` | `<#hex>` |

## Vanlige mønstre

`<Beskriv UI-mønstre som gjentas — f.eks. "bottom sheet for input", "haster-badge",
"tom-tilstand med illustrasjon". Hver: hva, når, hvordan.>`

## Tilgjengelighet

- Minimum kontrastforhold: `<f.eks. WCAG AA = 4.5:1>`
- Touch-targets: `<f.eks. minimum 44×44 px>`
- `<andre tilgjengelighetskrav>`

# Lessons — Database

Del av `tasks/lessons/`. Naviger via `index.md`.
Dekker: migrasjoner, skjemaendringer, constraints, foreign keys, seeding, Supabase.
→ se også: [[auth]] hvis oppgaven berører RLS eller permissions

---

*(Tom ved start. Append nye entries under denne linjen.)*

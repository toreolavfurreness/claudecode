# Lessons — UI

Del av `tasks/lessons/`. Naviger via `index.md`.
Dekker: komponenter, skjemaer, state-håndtering, navigation, lokalisering, stil.
→ se også: [[api]] hvis komponenten henter data

---

*(Tom ved start. Append nye entries under denne linjen.)*
